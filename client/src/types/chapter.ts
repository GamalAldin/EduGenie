// types/chapter.ts

export interface Chapter {
    _id: string;  // Unique identifier for the chapter
    name: string;  // Title of the chapter
    pdfUrl?: string;  // URL to the uploaded PDF (optional)
  }
  